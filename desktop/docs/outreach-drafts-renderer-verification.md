# F8 headless production renderer verification

2026-09-08. Explicit root source/build freeze and exclusive62611 lease. Built assets were `index-3AjsQjkp.js` and `index-CUPlC0pP.css`. The source tree SHA256 was `80eba346ae522c829b1c6113da3cbb10263f7960429b4347da11ea5eb119349f`; built renderer tree SHA256 was `531984539ce2f86a4d571f5d4c381061303a2d6b3fa5452454446c8f303de4e8`. Both were asserted unchanged after the accepted run.

The new opt-in `e2e/drafts-renderer.spec.ts` serves the built production renderer to managed headless Chromium and connects its named business bridge to production clients. HTTP is restricted to GET on the accepted local fixture. It reuses the prior F8 test-owned Activity, three-person batch and composition. Credentials remain in Node; trace/video are off, console/page errors are counted without recording their content, and physical ledgers contain sanitized route counts and hashes. No native Electron, IPC/Keychain implementation, real provider/model, SMTP or package acceptance is claimed.

The final actual run passed in5.9seconds (6.8seconds including runner), with42 GET requests and zero writes. It opened Match→the exact Activity→explicit Draft history→the existing three-person draft set. All three states remained visible; the completed second person displayed the actual server-rendered fixed email in a sandboxed iframe, including its original Toki signature. The iframe source contained the exact server HTML. No Send button was present.

An unsaved observation remained local across switching people, Back to people/Back to drafts, and Edit observation source→current F7 preparation→View creator→Back to activity→Back to drafts. The accepted run verified1320px and760px viewport layouts,135% root font size, reduced-motion preference, enabled keyboard focus/Tab progression and no document-width overflow. Durable composition data and Activity source context stayed unchanged; fixture control, model event and SMTP event files were byte-identical.

All page errors, console warnings/errors, unexpected bridge calls, unsafe HTTP attempts and blocked browser requests were0. Screenshots are retained under `desktop/output/playwright/drafts-renderer-F8-built-r-85e58-h-source-and-people-detours/`: `drafts-wide.png` and `drafts-narrow-135.png`. Root owns visual inspection of these artifacts.

Two preceding harness readiness failures are retained, not replaced by the passing result. `f8-renderer-be977ed0-61fd-4f0b-a0d7-de52ee6dd5a1.json` stopped at Activity loading because the harness queried button count before the list rendered. Adding an explicit rendered-list wait fixed the harness. `f8-renderer-37298cbe-8f0a-4b95-be15-c1c53b6f1f3d.json` passed the content/detour checks but attempted keyboard focus while Back-to-drafts was still loading. Waiting for the observation input to be enabled before focusing fixed the harness. No production source was changed for either failure.

The success physical ledger is `f8-renderer-2dec5995-389d-4059-a284-d4bfd14ee0d3.json` in the private fixture directory. At explicit lease handback source_fail/model_fail/hold were all none and queue depth was0. No further fixture HTTP was performed after handback. This read-only renderer evidence intentionally does not repeat persisted edit or sender-fact writes; those were covered separately through the production adapters and real worker in the F8 HTTP report.

## Final frozen rerun after visual inspection

Root inspected both initial screenshots and corrected history disclosure crowding, explicit-open heading focus, narrow remaining-width roster layout, and visible composition read errors. Root supplied a new source/build freeze and renewed exclusive lease. No earlier artifact was overwritten: the bounded rerun used `output/playwright-f8-final-20260908-2354`.

Final built assets: `index-BkabE4QR.js`, `index-P_y2iGTW.css`. Source SHA256 `c7046e8c357c6feb132da54aa7751c4b0469c776684331163423fabc959f0be8`; renderer bundle tree SHA256 `79c4a5e77df0671ef3352239492ebc37ccad0fff57e3be8dda2307eef9f3fbc6`. Both remained exact before/after the run. The existing headless path plus an explicit history `aria-expanded=false` assertion passed in4.5seconds (5.4seconds including runner):36 GET requests, zero writes, zero runtime/console/bridge/network errors. Full N, exact iframe, all local-dirty detours, narrow135%/reduced-motion/keyboard and unchanged durable data remained verified.

Final screenshots are `desktop/output/playwright-f8-final-20260908-2354/drafts-renderer-F8-built-r-85e58-h-source-and-people-detours/drafts-wide.png` and `drafts-narrow-135.png`. Final private ledger: `f8-renderer-a6345b84-0ee9-4400-8c22-56f04e560af7.json`. Root owns final visual inspection. Source/model/SMTP controls/events remained unchanged, queue depth was0, and the lease was explicitly returned again after this run. No further fixture HTTP followed this final handback.
