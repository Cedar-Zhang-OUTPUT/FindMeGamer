# F8 production adapter HTTP verification

2026-09-08, exclusive coordinator fixture lease on loopback62611. Backend B `ece2e9d9558dfe057dc40ad58bd98a86e3149dd5`, actual migration `20260908_0017`; the F8 draft routes/schema are immutable A `5ffd7c47f9f7e91f428449955962b1bed05fe467`. The new opt-in `e2e/drafts-real-api.spec.ts` uses the production Match, Creator, Outreach and Drafts clients. Private credentials are read only in Node from the handed-off0600 configuration. No browser, native shell, Keychain, real provider/model, sending/qualification API, or SMTP call is involved.

The first actual run failed after creating one labeled Activity, one query and one selection. Existing CreatorClient rejected imported Work `source_fields.text`. The original physical ledger is retained privately as `f8-drafts-aab938e3-f9a6-4b2c-95cd-fc3e2422e5e0.json`; no failed setup was deleted or hidden.

Two bounded frontend compatibility fixes were then independently authorized against the immutable schema. WorkDetail `source_fields` is PublicJSONObject: a source-only20,000-character string `text` is now preserved, while editable WorkFields and manual overrides remain strict. Fractional finite nonnegative Work timestamps are accepted by draft snapshot validation, matching the backend float contract. Both regression tests failed before their fixes; the focused Creator/Drafts client suite then passed53 cases. Unsupported source text types/oversize, attempted text writes, and negative/nonfinite timestamps stay rejected.

The second actual run reused the exact first-run Activity/query and passed in5.6seconds (6.2seconds including runner). It used existing synthetic Creator/work evidence read-only and created its own three selections and one immutable recipient batch. Canonical registration and one custom immutable version were checked through per-game listing and durable reads. One composition retained all three people and original order, including missing email and missing observation evidence. Same-body/key creation replay returned the same composition and history total remained one.

Actual worker→strict synthetic model HTTP events were `drafting:200` twice and `drafting:503` once. The first draft succeeded without selected email. An explicit source repair and refresh of the second draft produced the controlled failure; restoring controls and explicitly retrying only that member succeeded while retaining the first success. Manual four-value editing, explicit two-member sender facts, source-change invalidation, and explicit refresh clearing values/confirmations passed. Original recipient snapshots and Activity source context remained byte-equivalent under JSON serialization. Final states were `needs_repair`, `succeeded`, `needs_repair`; all send_ready flags remained false. Existing SMTP events were byte-identical.

The success physical ledger `f8-drafts-c9b7fa4d-7748-4ebf-a3ea-d8eb81bbd13f.json` is retained in the private fixture directory beside the failure ledger. It contains public IDs, sanitized route-family request counts, stage, side effects and fixture event counts; no raw responses, addresses, credentials or full email bodies. Both runs restored source_fail/model_fail/hold to none in finally. At handback queue depth was0, and the exclusive fixture lease was explicitly returned to root. Typecheck passed. A subsequent source-only hardening added explicit owned Activity/Creator/composition/draft ID checks to the HTTP allowlist; no further HTTP was run after handback.

This verifies production adapter→API→worker→model fixture→durable data behavior. Renderer integration, native Electron behavior and final package acceptance remain separate; this report makes no claim about them.
