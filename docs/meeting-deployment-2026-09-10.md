# Meeting-flow backend deployment — 2026-09-10

## Release

- Production: `https://44.233.174.193`.
- Exact deployed commit: `684a12078ae0a22475f5fa77532b44f2d6e2d026`; business changes end at `29fe65b`.
- Previous deployment: `17aad383e1965b639eb88151493e44cc7165b2b7`.
- Authorized maintenance window; API, Worker and Beat stopped before backup/migration. No rolling-worker compatibility assumption.
- Alembic upgraded `20260908_0019` → `20260909_0020` → `20260909_0021` successfully.
- API, Worker, Beat, PostgreSQL, Redis and Caddy all healthy after deployment. HTTPS readiness succeeded.

## Preservation and recovery evidence

Before/after read-only deterministic row fingerprints matched for 17 tables: game_profiles, creator_profiles, creator_contacts, creator_works, analysis_jobs, match_tasks, service_secrets, activities, discovery_plans, discovery_queries, discovery_batches, discovery_candidates, activity_selections, deliveries, activity_deliveries, send_batches and activity_send_batches. Activity fingerprints exclude only the three newly added columns; existing rows correctly received null campaign_brief, revision 0 and initial_selection_initialized=true.

Existing records retained: 1 Game, 22 Creators, 29 contacts, 264 works, 32 analysis jobs, 3 legacy Match tasks, 3 Activities, 3 plans, 2 queries and 2 discovery batches. No selection or delivery records existed before migration, and none were generated by the checks.

- Pre-migration S3 object: `backups/20260910T035632Z-684a12078ae0-pre-migration.dump` in `zhangyue-data-493392056671-us-west-2`.
- Dump and companion `.sha256` downloaded back to the server; checksum verification passed. This round did not perform an isolated full database restore drill.
- Previous API/Worker/Beat images retained as `find-me-gamer-{service}:rollback-17aad38`.
- Previous Git commit retained as `rollback-meeting-17aad38` in `/opt/find-me-gamer`.
- Root-only configuration archive: `/var/backups/find-me-gamer/20260910-meeting/config.tgz` (0600, containing app.env/master.key). Original configuration files remain root-owned 0600.
- Rollback artifacts are retained, not an assertion that a rollback was executed. Any database restore requires a maintenance window and consideration of subsequent colleague writes; do not overwrite live data casually.

## Live verification

Authenticated HTTPS reads passed for readiness, session, reanalysis settings, Activities, the previously failed plan (still failed, not reset), both Library lists and all 1 Game + 22 Creator detail endpoints.

The previously authorized X app-only token was read from local Keychain without logging and migrated through the existing authenticated encrypted service-settings API. After migration, the original DeepSeek/Google AI/YouTube secret rows retained their exact pre-deployment fingerprint; X is the only added service. SMTP remains unconfigured.

Exactly three bounded official X reads ran from the deployed API container using its stored encrypted credential:

1. Recent search, `"Minecraft" -is:retweet`, page size 10: 10 posts, 10 accounts, no issues. Next-page cursor present but not followed.
2. One returned account lookup: identity matched; follower count present.
3. That account's recent original posts, max_results 5: 5 posts, all matching the account. No pagination.

These checks prove cloud credential/network access to the three official reads, not a new full persisted X analysis/AI campaign run. Existing bounded automated tests cover those application workflows. No real SMTP, paid-model calls, automated pagination, top-ups, test Profile imports or synthetic sender identity were introduced during deployment. Twitch/Instagram live integration remains outside this release's verified scope.

## Scope and handoff

The internal.4 desktop artifact and frontend gate are owned by the coordinator/frontend task. This backend deployment does not itself publish a desktop installer. Existing accepted business tests and bounded reviews were not repeated or expanded. The EC2 operations skill guided preservation of the existing instance/role and explicit rollback artifacts; no new infrastructure or IAM permissions were introduced.
