# CLI service rollout — 2026-09-15

User approved deploying the service and publishing source/CLI/Skills after local acceptance. This record supersedes the earlier pre-deployment stopping boundary; the pre-deployment acceptance report remains a dated record.

## Deployment

- Existing EC2 / verified HTTPS base: `https://44.233.174.193` (do not append `/v1` in CLI configuration).
- Code/image: new-service implementation at `cda2de5`, image `fmg-agent:0.1.0-cda2de5`; server checkout `/opt/fmg-agent-0.1.0-cda2de5`.
- Dedicated `find_me_gamer_agent` database and `fmg_agent` role; schema `0004_usage`. No Library/Profile/Match history migration.
- New `fmg-agent-api-1` and `fmg-agent-worker-1`; old macOS API/Worker/Beat remain stopped. Existing PostgreSQL, Redis and Caddy reused.
- Provider configuration was decrypted only in a transient server-side old-image process and transferred into root-private `/etc/fmg-agent/service.env`. Neither credentials nor access tokens are present in source, archives or this report.
- YouTube, X, Google AI configured; Gemini `gemini-3.8-flash`. Steam Store helpers need no key; key-requiring Steam Web API operations are not enabled. SMTP remains unconfigured.
- Added `/v1/*` proxy route. Found an existing stale bind-mounted Caddyfile inode; restarted only the proxy during the approved maintenance window, then verified public HTTPS/auth. Preserved prior proxy file in the new-service backup directory.

## Live verification

Verified through the CLI using the public HTTPS address and a dedicated user token:

- Authentication success; read-only token denied preview access; revoked token rejected.
- YouTube `search.list`: one result requested/returned.
- X `searchPostsRecent`: returned posts and expanded author information; actual quota/permission accepted the request.
- Steam `store.search` and `store.recommendations`: returned data.
- Fixed template catalog and actual rendered preview: success, send readiness false because SMTP is absent. **No send endpoint called; no real email sent.**
- One asynchronous Hollow public-business-contact job: public-page checkpoint completed, then Gemini fallback completed with one source-labelled email. This is not an inbox-deliverability guarantee.
- Per-run ledger: 5 upstream calls (YouTube 1, X 1, Steam 2, Gemini 1). Gemini reported total 1,267 tokens. Monetary cost and Codex task usage remain unknown, rather than inferred from account balances.

Fresh pre-rollout regression: 108 new-service tests passed; Go tests/vet passed. Additional deployment guard tests pass. One test invocation initially used the wrong relative CLI binary path; rerun with an absolute path passed. No product code change was needed for that test invocation.

## Backups

Original `/var/backups/find-me-gamer/pre-cli-20260915` dump/config/image checksums all verified before deployment. Those backups remain untouched.

New service backup successfully ran through PostgreSQL 17 custom-format dump and upload to `s3://zhangyue-data-493392056671-us-west-2/backups/agent/`. `fmg-agent-backup.timer` runs daily around 03:20 UTC with up to 10 minutes jitter. Service exit status was zero and timer was enabled. Existing S3 `backups/` 30-day lifecycle applies. Local backup copies are retained; monitor disk usage rather than silently deleting them.

The checked-in systemd unit is this deployment's concrete configuration, not a generic installer. On a later version, update its checkout/image paths before enabling it. It uses the existing instance role, not embedded AWS credentials. Public entry point rollback and database restore procedure remain in `deploy/agent-services/rollback.md`.

## Release handoff

CLI tag: `fmg-v0.1.0`, separate from legacy macOS DMG releases. Four platform archives, both Skills, installer and SHA256SUMS are published together after candidate checks. Source is retained on `codex/native-profile-editing` and the release tag; no force push or main-branch rewrite is required. The CLI release must not replace the legacy macOS latest-release designation.

New machines need macOS/Linux arm64/amd64, Python 3.10+ for installation, installed Skills and a privately supplied revocable gateway token. They do not need company YouTube/X/Google credentials. Reload Codex's Skill inventory/start a fresh conversation after installation. Real SMTP delivery remains a separate future setup/acceptance step.

Publication completed: https://github.com/Cedar-Zhang-OUTPUT/FindMeGamer/releases/tag/fmg-v0.1.0 . It is an internal prerelease, not a draft, and does not replace the macOS latest release. Release tag points to `fc80a84cd6ea586c84abf09383c644f94745d7a6`.

Post-upload acceptance downloaded all seven assets again and verified every SHA256. An anonymous explicit-tag online install into a fresh temporary directory installed the macOS arm64 CLI and both Skills; the installed CLI then completed its real GitHub upgrade path and production auth/catalog/template smoke. Downloaded Skills passed their validators. The released Linux amd64 binary also executed on EC2 and reported 0.1.0. No global user CLI/Skill installation was overwritten in this verification.
