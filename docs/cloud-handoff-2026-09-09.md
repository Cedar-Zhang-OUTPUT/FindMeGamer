# PRD v2 backend deployment — 2026-09-09

## Deployed target

- Origin: `https://44.233.174.193` (existing trusted IP TLS).
- EC2: `i-041e77ab08c86ff1d`, `us-west-2`, existing `zhangyue-ec2` role.
- Backend commit: `5706ad76f924991b80ee2a7fb6806528366be5ce`.
- Previous checkout: `77d6f234de46942f769afb2c0a9d05b18db994ab`.
- Actual PostgreSQL migration: `20260908_0019`, upgraded from `20260904_0007`.
- API, Worker, Beat, PostgreSQL, Redis and Caddy all healthy after deployment.

This was the approved single-server maintenance-window update, not a new Library
or a no-downtime migration. No network/IAM changes, new services, credential
replacement, real model/platform test calls or SMTP sends were made.

## Preflight and preserved data

The SSH key supplied by the operator was corrected from0644 to0600 without reading
its contents. The server checkout was clean and used the existing local bare Git
mirror. Protected `app.env` and `master.key` were both root-owned0600; disk had33GiB
available. Backup timer was active. Analysis and Match jobs were terminal, Redis
queue empty, and no send batches/deliveries existed before maintenance.

After migration, SHA-256 comparisons of canonical serialized database selections
matched their preflight values for:

- 22 Creator IDs, YouTube IDs, source facts, analysis, Brief and analysis timestamps;
- one Game ID, Steam ID, source facts, analysis, Brief and analysis timestamp;
- 32 analysis job IDs/statuses/Profile associations (25 succeeded, seven failed);
- all three complete encrypted service-configuration rows.

All three existing Match tasks were succeeded before maintenance. No task reset,
Library deletion or new acquisition was performed. New Activity tables start empty;
this does not erase existing legacy Match history. Final active-analysis count and
Redis queue depth were zero; delivery count remained zero.

## Backups and rollback evidence

Bucket: `zhangyue-data-493392056671-us-west-2`.

1. `backups/20260909T050805Z-77d6f234de46-pre-migration.dump` and `.sha256`:
   uploaded with AES256 encryption, downloaded, checksum verified, and restored
   successfully into a separate temporary database before migration. Verified
   old migration and counts: Game1, Creator22, Analysis32, Match3, Campaign3,
   Delivery0. The rehearsal database was then removed, not the production database.
2. `backups/20260909T051327Z-5706ad76f924-pre-migration.dump` and `.sha256`:
   generated by the deployment script with Proxy/API/Worker/Beat stopped, **before
   migration**. The filename contains the new checkout SHA, but its database schema
   is still0007. Downloaded again after deployment, SHA-256 checked successfully;
   `pg_restore --list` parsed155 lines. This exact second backup was not subjected
   to a second full restore rehearsal.
3. Root-only local copies of the second backup/checksum plus `config.tgz` are under
   `/var/backups/find-me-gamer/20260909-prd-v2/`. The config archive contains the
   original `app.env` and master key and must never be published or logged.
4. Previous images retained as `find-me-gamer-api:rollback-77d6f23`,
   `find-me-gamer-worker:rollback-77d6f23`, and
   `find-me-gamer-beat:rollback-77d6f23`. Git ref
   `rollback-20260909-77d6f23` retains the previous checkout.

If rollback becomes necessary, first stop all application writers and back up any
post-release changes. Restore the pre-migration dump into a separate database and
verify it before an explicitly authorized database switch; use the old checkout
and retained images together. Do not simply run old workers against0019 or use a
destructive downgrade to discard v2 data. No rollback was required or executed.

## Source delivery

An exact5706ad7 Git bundle was imported into the existing
`/opt/find-me-gamer-source.git` mirror under `deploy-backend-5706ad7`; no main branch
was force-pushed. Cross-border SCP was slow. An additional transfer used the same
instance role to issue a600-second, single-object S3 PUT URL for
`backups/releases/20260909-backend-5706ad7.bundle`. The signed URL was consumed only
in process memory, never shown or saved. An initial default-signature upload failed;
explicit SigV4 succeeded. No AWS access key or broader permissions were introduced.
The code-only bundle remains under the existing backup-prefix lifecycle.

Local and server bundle SHA-256 both matched:
`cb3444cc26fe702b8eaff6bf57f8cc1ac9de1f5dc056a1755cf139b60b70bd26`.
Git bundle verification succeeded before mirror import. The reviewed existing
`ops/deploy.sh` built the fixed target, stopped writers, created the second backup,
migrated0007→0019 and restarted services. Protected config was not replaced.

## Acceptance and remaining dependencies

Public HTTPS readiness and authenticated session passed. Read-only endpoints for
reanalysis settings, three configured provider states, v2 Game/Creator Library,
Activities, changed jobs and legacy Match history returned200. An initial probe
used the nonexistent `/api/v1/settings` root and returned404; it was corrected to
the documented `/api/v1/settings/reanalysis` endpoint, which passed. No business
code was changed to mask that probe mistake.
All23 existing v2 Profile detail endpoints (one Game and22 Creators) were then
read individually over authenticated HTTPS and returned200.

DeepSeek, YouTube and Google AI configuration survived, with their prior successful
test flags. Those flags are historical, not new live provider acceptance. **X has
no cloud credential yet**, and **SMTP remains unconfigured**. Do not claim real X
analysis, new model-quality verification, or external delivery from this deployment.
Twitch/Instagram remain the approved unsupported/preset boundaries.

Client packaging/GitHub upload and user-directed live platform tests belong to the
coordinator, not this deployment unit. Existing isolated frontend fixtures were not
accessed or changed. The complete backend suite was not redundantly rerun here;
this gate is actual migration, data preservation, service and read-only API checks.
